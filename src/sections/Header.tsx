import NavBar from '@/components/NavBar';

export const Header = () => {
    return (
        <header className="sticky top-0 z-50 backdrop-blur-sm">
            <NavBar />
        </header>
    );
};
 